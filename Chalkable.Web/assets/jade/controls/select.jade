mixin Select(name, hiddenName, controller, action, params)
    -attributes.name = name
    -attributes = self.processAttrs(attributes, hiddenName);
    -attributes['data-controller'] = controller
    -attributes['data-action'] = action
    -attributes['data-params'] = params
    if hiddenName
        +Hidden(hiddenName)(class = name + '-hidden').hidden-select-value
    select(attributes).control-input
        if(attributes.items)
            each item in attributes.items
                +Option(item["get" + attributes.idParam.toUpperCase()], item["get" + attributes.nameParam.toUpperCase()])
        else
            block

mixin Option(value, displayName, selected)
    -attributes.value = value
    -attributes.selected = selected
    option(attributes)=displayName
