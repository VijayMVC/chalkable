mixin CloseOpen(title)
    -attributes = self.processAttrs(attributes)
    .close-open-control(attributes).opened.finished
        h3=title
            .open=Msg.Open
            .close=Msg.Close
        .close-open-block
            block