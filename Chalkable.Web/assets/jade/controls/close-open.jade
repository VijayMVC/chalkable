mixin CloseOpen(title)
    -attributes = self.processAttrs(attributes)
    .close-open-control(attributes).co-opened.finished
        h3=title
            .co-open=Msg.Open
            .co-close=Msg.Close
        .close-open-block
            block