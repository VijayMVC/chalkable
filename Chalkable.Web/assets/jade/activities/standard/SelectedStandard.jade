.item-block.selected-item.pressed(
    data-tooltip=self.getTooltip()
    data-id=self.getId().valueOf(),
)
    .item-name=self.getName()
    if self.getDescription()
        .item-description= (self.getName() ? '  -  ' : '') + self.getDescription()