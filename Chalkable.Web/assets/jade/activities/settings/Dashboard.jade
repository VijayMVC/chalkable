.settings
    .row
        .item
            +ActionLink('a', 'b') Change password
        .item
            if self.isPreferencesVisible()
                +ActionLink('a', 'b') Preferences
        .item
            if self.isAppCategoriesVisible()
                +ActionLink('appcategories', 'list') Application categories
    .row
        .item
            if self.isDepartmentsVisible()
                +ActionLink('departments', 'list') Departments
        .item
            if self.isStorageMonitorVisible()
                +ActionLink('storage', 'list') Storage monitor
        .item
            if self.isBackgroundTaskMonitorVisible()
                +ActionLink('backgroundtask', 'list') Background task monitor



