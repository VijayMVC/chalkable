+SchoolPersonSettings()
    .left
        +ActionLink('settings', 'generalStudent').pressed General
    .right
        +ActionLink('students', 'info', self.personId).profile-toolbar-button Profile