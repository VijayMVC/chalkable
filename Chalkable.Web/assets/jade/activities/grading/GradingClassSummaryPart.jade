.mps-container
    each item,i in self.getItems()
        div(class=(i==0) ? "open" : "", data-index=i).marking-period-container
            div(data-tooltip=Msg.Avg + " " + (item.getAvg() || 0)).mp-title=Msg.Marking_Period + ' ' +  item.getMarkingPeriod().getName()
                .collapse
            .mp-data
                .ann-types-container
                    +LeftRightToolbar(item.getByAnnouncementTypes(), chlk.templates.grading.GradingClassSummaryItemTpl)(
                        needDots = true,
                        roundedDots = true
                    )