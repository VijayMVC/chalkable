-users=self.getUsers()
-selectedIndex = self.getSelectedIndex();
.people-list-container.not-transparent
    .buttons
        .right
            BUTTON.total-count= self.getTotalText()

    .buttons.filter
        .left
            .buttons-group
                BUTTON(class=self.isByLastName()?"":"pressed", data-value="false") First name
                BUTTON(class=self.isByLastName()?"pressed":"", data-value="true") Last name
            input(type="hidden", name="byLastName", value=(self.isByLastName() ? 'true' : 'false'))
        .right
            input(placeholder='Filter list...', name="filter", value=self.getFilter()).people-search
            .people-search-imgs
                label.people-search-img
                label.people-search-close.opacity0
        .clear-fix
    .grid-container.loader-container
        +RenderWith(self.getModel(), chlk.templates.people.UsersGridTpl)(
            userRole = self.getUserRole(),
            currentUser = self.getCurrentUser(),
            rolesText = self.getRoleText()
        )



