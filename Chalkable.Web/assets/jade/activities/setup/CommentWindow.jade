.dialog.report-window
   +ActionForm('setup', 'submitComment').comment-form.no-working.report-form
      .x-window-header
         .close.close-btn.dark
         h1=self.getCommentId() ? Msg.Edit_comment + ': ' + self.getName() : Msg.Create_comment

      .x-window-body
         +Hidden('commentid', self.getCommentId() && self.getCommentId().valueOf())
         .report-content
            TABLE.report-table
               TR
                  TD.item-1
                     h3=Msg.Comment
                     input(name="comment", type="text", value=self.getComment(), readonly=self.isSystem())

      .x-window-footer.report-bottom
         BUTTON(type="submit").chlk-button.blue-button=Msg.OK
         BUTTON(type="button").close.chlk-button.grey-button=Msg.Cancel