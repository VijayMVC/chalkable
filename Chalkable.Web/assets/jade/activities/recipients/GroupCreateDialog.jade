.dialog.recipients-window
   .x-window-header
      .close.close-btn.dark
      h1=self.getPageTitle()
   +ActionForm('group', 'createGroupSubmit').recipients-body
      .group-name-cnt
         LABEL Group Name
         INPUT(name="name", type="text", placeholder="Group Name...").name
      +GroupPeopleSelector(self, self.getRequestId())