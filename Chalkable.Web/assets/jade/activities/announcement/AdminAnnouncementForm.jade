.announcement-form.admin-announcement-form.loader-container
    .loader
    +ActionForm('announcement', 'save')(onlySubmitValidate=true).announcement-form
        .ann-form-container
            +BaseAnnouncementFormContainer(self.getAnnouncement(), null, self.getAdminRecipientId(), self.getAdminRecipients(), self.getUserRole(), self.isCanAddStandard())
    +AddEditReminderForm(self.getAnnouncement(), self.reminders)