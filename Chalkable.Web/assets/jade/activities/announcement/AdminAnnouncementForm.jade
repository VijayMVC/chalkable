-var announcement = self.getAnnouncement()
-var state = announcement && announcement.getState()
.announcement-form.loader-container
   if announcement
      h2.page-title Admin Announcement
      .loader
      +ActionForm('announcement', state ? 'save' : 'saveOnCreate')(onlySubmitValidate=true).no-working
         .ann-form-container
            +BaseAnnouncementFormContainer(self, announcement, null, null, null, self.getUserRole(), null).announcement-form
