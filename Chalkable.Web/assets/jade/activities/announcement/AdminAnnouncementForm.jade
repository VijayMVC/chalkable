.announcement-form.admin-announcement-form.loader-container
    .loader
    +ActionForm('announcement', 'save')(onlySubmitValidate=true)
        .ann-form-container
            +BaseAnnouncementFormContainer(self.getAnnouncement(), null, self.getAdminRecipientId())
    +AddEditReminderForm(self.getAnnouncement(), self.reminders)