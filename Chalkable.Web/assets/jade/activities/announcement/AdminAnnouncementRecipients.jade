-var recipients = self.getRecipients() || []
+ActionButton('announcement', 'showGroups', self.getId()).add-recipients-btn.grey-button.same-on-hovere +
each recipient in recipients
      .grey-button.same-on-hover.button-like=recipient.getGroupName()
            +ActionLink('announcement', 'removeRecipient', recipient.getAnnouncementId(), recipient.getGroupId()).remove-recipient