-var name = self.getTitle()
li(data-title = name, data-value = name)
    +ActionLink('announcement', 'createFromTemplate', self.getId())(data-tooltip=name, data-tooltip-type="overflow").create-from-template= name
