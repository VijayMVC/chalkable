.teacher-info.info-page.view-mode.loader-container
    .loader
    +ProfileTopBar(self.getActionLinksData())
    +ProfileInfoView(self.user)
        //todo parent section
    +ProfileInfoForm('students', self.user)
